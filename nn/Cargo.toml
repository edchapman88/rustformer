[package]
name = "nn"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
micrograd = { path = "../micrograd" }
matrix_library = { path = "../../matrix_library" }
rand = "0.8.5"
statrs = "0.16.0"